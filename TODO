# TODO

Here's a current list of todo items, more or less ordered by priority.

 - Ontology interface.
 - Minimal handling of erroneous sensitivity analysis rules.
 - Rule atom filtering.
 - Make the parser configuration automatically configurable (based on keys). To achieve this, split the configuration in rule syntax and predicate syntax.
 - Introduce typed literals.
 - Unify the logical entities interface.
 - Documentation.
